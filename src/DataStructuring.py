

class structure():
    def __init__(self):
        pass

    def createDataFrame(self, loader, params: dict):

        params.keys()
        params.values()

        for key, value in params.items():
            print(params.items())

        loader.load_data(params)
        pass
        # Create a DataFrame from the data

    